version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: events-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: eventsdb
      MYSQL_USER: eventsuser
      MYSQL_PASSWORD: events123
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 20
    command: --default-authentication-plugin=mysql_native_password

  app:
    image: nourheneayari/eventsproject:latest
    container_name: eventsproject-app
    ports:
      - "8089:8089"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://events-mysql:3306/eventsdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&autoReconnect=true
      SPRING_DATASOURCE_USERNAME: eventsuser
      SPRING_DATASOURCE_PASSWORD: events123
    depends_on:
      mysql:
        condition: service_healthy
    restart: unless-stopped

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: events-phpmyadmin
    ports:
      - "8082:80"
    environment:
      PMA_HOST: events-mysql
    depends_on:
      - mysql

volumes:
  mysql-data:
